# RecToSpec - 要件定義書 v2.4

> プロジェクトナレッジ管理詳細版
最終更新: 2025年12月30日
> 

---

## プロジェクト概要

### 正式名称

**RecToSpec** （レクトスペック）

```
RecToSpec - From Browser Recording to Test Specification
ブラウザ記録から、テスト仕様へ
```

### 名称の由来

| 要素 | 意味 |
| --- | --- |
| **Rec** | Recording（記録） |
| **To** | 変換を示す |
| **Spec** | Specification（仕様） |

### コンセプト

Chrome Recorderで記録したブラウザ操作から、Gherkin形式のテストケースを経て、実行可能なPlaywrightテストコードまでを自動生成するOSSツール。

---

## Why RecToSpec?

品質の高いテストコードを作るには、テスト設計の知識・フレームワークの知識・コーディング能力など、多様なスキルが必要です。

- **開発エンジニア**: コーディングはできるが、適切なテストケース設計が難しい
- **QAエンジニア**: テスト設計はできるが、テストコードの実装が難しい

どちらも高水準な人材は稀です。

RecToSpecは、この両者のスキルギャップをAIで橋渡しします。

- QAエンジニアがテスト設計（Gherkin）を行い、AIが実装可能なコード（Playwright）を生成
- 開発エンジニアが記録した操作から、AIが適切なテストケース（Gherkin）を提案

**誰もが高品質なテストを実現できる**ようになります。

---

## AI SDK・LLM選定

### 決定事項: Vercel AI SDK採用

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     AI SDK アーキテクチャ                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  RecToSpec                                                              │
│  └─ Vercel AI SDK (ai パッケージ)                                      │
│       ├─ 統一API: generateText() / generateObject()                    │
│       │                                                                 │
│       └─ プロバイダー（プラグイン形式で追加可能）                      │
│            ├─ @ai-sdk/google    ← Phase 1 (MVP)                        │
│            ├─ @ai-sdk/anthropic ← Phase 1 (MVP)                        │
│            ├─ @ai-sdk/xai       ← Phase 2                              │
│            └─ @ai-sdk/openai    ← Phase 2                              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

```

### Vercel AI SDK採用理由

| 観点 | メリット |
| --- | --- |
| **統一API** | 全プロバイダーで同じ `generateText()` を使用 |
| **プロバイダー切り替え** | 2行のコード変更で別LLMに切り替え可能 |
| **Node.js対応** | CLIツールで問題なく動作（React不要） |
| **TypeScript** | 完全な型サポート |
| **軽量** | LangChainより圧倒的に軽量 |
| **アクティブ開発** | 週40,000+ npmダウンロード、継続的なアップデート |

### LLM対応優先順位

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     LLM対応ロードマップ                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  優先度1: Google Gemini                           ← Phase 1 (MVP)      │
│  ├─ 理由: 無料利用枠が充実（1,500 requests/day）                       │
│  ├─ 推奨モデル: gemini-2.5-flash（コスト効率最優先）                   │
│  └─ 代替モデル: gemini-2.5-pro（高精度が必要な場合）                   │
│                                                                         │
│  優先度2: Anthropic Claude                        ← Phase 1 (MVP)      │
│  ├─ 理由: コード生成能力が最高水準、日本語対応優秀                     │
│  ├─ 推奨モデル: claude-sonnet-4                                        │
│  └─ 代替モデル: claude-haiku-3.5（コスト重視時）                       │
│                                                                         │
│  優先度3: xAI Grok                                ← Phase 2            │
│  ├─ 理由: 圧倒的な低価格（$0.20/$0.50 per 1M tokens）                  │
│  ├─ 推奨モデル: grok-4.1-fast                                          │
│  └─ 注意: 日本語対応が限定的                                           │
│                                                                         │
│  優先度4: OpenAI GPT                              ← Phase 2            │
│  ├─ 理由: 業界標準、既存ユーザーが多い                                 │
│  ├─ 推奨モデル: gpt-4o                                                 │
│  └─ 代替モデル: gpt-4o-mini（コスト重視時）                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

```

### LLM価格比較（2025年12月時点）

| 優先度 | プロバイダー | モデル | Input (per 1M) | Output (per 1M) | 無料枠 |
| --- | --- | --- | --- | --- | --- |
| 1 | Google | Gemini 2.5 Flash | $0.075 | $0.30 | ✅ 1,500 req/day |
| 1 | Google | Gemini 2.5 Pro | $1.25 | $5.00~$10.00 | ✅ 制限あり |
| 2 | Anthropic | Claude Sonnet 4 | $3.00 | $15.00 | ❌ |
| 2 | Anthropic | Claude Haiku 3.5 | $1.00 | $5.00 | ❌ |
| 3 | xAI | Grok 4.1 Fast | $0.20 | $0.50 | ❌ |
| 3 | xAI | Grok 4 | $3.00 | $15.00 | ❌ |
| 4 | OpenAI | GPT-4o | $2.50 | $10.00 | ❌ |
| 4 | OpenAI | GPT-4o-mini | $0.15 | $0.60 | ❌ |

### コスト試算（1フロー変換あたり）

| プロバイダー | モデル | Gherkin生成 | Playwright生成 | 合計 |
| --- | --- | --- | --- | --- |
| Google | Gemini 2.5 Flash | ~$0.001 | ~$0.002 | **~$0.003** |
| Anthropic | Claude Sonnet 4 | ~$0.03 | ~$0.07 | ~$0.10 |
| xAI | Grok 4.1 Fast | ~$0.001 | ~$0.003 | ~$0.004 |
| OpenAI | GPT-4o | ~$0.02 | ~$0.05 | ~$0.07 |

※ Gemini無料枠を使用すれば、1日1,500回まで無料で変換可能

---

## アーキテクチャ

### 決定事項: CLIツールとして提供

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     RecToSpec アーキテクチャ                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌───────────────────┐    ┌─────────────────────────────────────────┐  │
│  │ Chrome Recorder   │───▶│ RecToSpec CLI                           │  │
│  │  └─ JSONエクスポート │    │  ├─ JSONパース                         │  │
│  └───────────────────┘    │  ├─ Vercel AI SDK                       │  │
│                           │  │   ├─ Gherkin生成                      │  │
│                           │  │   └─ Playwright生成                   │  │
│                           │  ├─ エディタ（VSCode連携 or WebUI）      │  │
│                           │  └─ 設定管理                             │  │
│                           └─────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

```

### 採用理由

| 観点 | CLIツールの利点 |
| --- | --- |
| **APIキー管理** | 環境変数でローカル管理、セキュリティが高い |
| **開発コスト** | Chrome拡張の審査対応・Manifest V3対応が不要 |
| **ターゲットユーザー** | QAエンジニア・開発者はCLIに慣れている |
| **Chrome Recorder連携** | 標準のJSONエクスポートで十分 |

---

## 全体フロー

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         RecToSpec ワークフロー                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Step 1: Chrome Recorderで記録 & エクスポート                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Chrome DevTools > Recorder > Record                            │   │
│  │  → 操作記録                                                      │   │
│  │  → Export as JSON → login-flow.json                             │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              │                                          │
│                              ▼                                          │
│  Step 2: CLIでGherkin生成                                               │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  $ rectospec generate login-flow.json                           │   │
│  │                                                                  │   │
│  │  🔄 Analyzing recording...                                       │   │
│  │  🤖 Generating Gherkin with Gemini...                            │   │
│  │  ✅ Created: login-flow.feature                                  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              │                                          │
│                              ▼                                          │
│  Step 3: エディタで編集                                                 │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Option A: VSCode                                                │   │
│  │  $ code login-flow.feature                                       │   │
│  │                                                                  │   │
│  │  Option B: 内蔵Webエディタ                                       │   │
│  │  $ rectospec edit login-flow.feature                             │   │
│  │  → Opens http://localhost:3000/editor                            │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              │                                          │
│                              ▼                                          │
│  Step 4: Playwrightコード生成                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  $ rectospec compile login-flow.feature --output ./tests        │   │
│  │                                                                  │   │
│  │  🤖 Generating Playwright code...                                │   │
│  │  ✅ Created:                                                      │   │
│  │     - tests/pages/LoginPage.ts                                   │   │
│  │     - tests/specs/login.spec.ts                                  │   │
│  │     - tests/fixtures/login-data.ts                               │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              │                                          │
│                              ▼                                          │
│  Step 5: テスト実行                                                     │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  $ cd tests && npx playwright test                               │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

```

---

## CLI コマンド設計

### インストール

```bash
npm install -g rectospec

```

### コマンド一覧

```bash
# 初期設定（APIキー設定、プロバイダー選択）
rectospec init

# Recording JSONからGherkin生成
rectospec generate <recording.json> [options]
  --output, -o    出力ファイルパス
  --lang          言語設定 (ja/en)
  --edge-cases    エッジケースシナリオを生成 (default: true)
  --provider, -p  LLMプロバイダー (google/anthropic/xai/openai)
  --model, -m     モデル名

# GherkinからPlaywright生成
rectospec compile <feature-file> [options]
  --output, -o    出力ディレクトリ
  --framework     テストフレームワーク (playwright/cypress/selenium)
  --typescript    TypeScript出力 (default: true)
  --provider, -p  LLMプロバイダー
  --model, -m     モデル名

# 内蔵Webエディタを起動
rectospec edit [feature-file]
  --port          ポート番号 (default: 3000)

# ワンショット変換（JSON → Playwright）
rectospec quick <recording.json>
  # Gherkin編集をスキップして直接コード生成

# 設定確認・変更
rectospec config [key] [value]
  # 例: rectospec config provider anthropic
  # 例: rectospec config model claude-sonnet-4

# バージョン確認
rectospec --version

# ヘルプ
rectospec --help

```

### 使用例

```bash
# 基本的な使い方（デフォルト: Gemini）
$ rectospec generate ./recordings/login.json
✅ Created: login.feature

# プロバイダー指定
$ rectospec generate ./recordings/login.json --provider anthropic
✅ Created: login.feature (using Claude)

$ rectospec compile login.feature -o ./tests
✅ Created: tests/pages/LoginPage.ts
✅ Created: tests/specs/login.spec.ts

# ワンショット変換
$ rectospec quick ./recordings/checkout.json -o ./tests
✅ Created: tests/pages/CheckoutPage.ts
✅ Created: tests/specs/checkout.spec.ts

# 内蔵エディタで編集
$ rectospec edit login.feature
🌐 Editor running at http://localhost:3000

```

---

## APIキー管理

### セキュリティ設計

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     APIキー管理の優先順位                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. 環境変数（最優先・推奨）                                            │
│     $ export GOOGLE_GENERATIVE_AI_API_KEY=...    # Gemini              │
│     $ export ANTHROPIC_API_KEY=sk-ant-...        # Claude              │
│     $ export XAI_API_KEY=...                     # Grok                │
│     $ export OPENAI_API_KEY=sk-...               # GPT                 │
│                                                                         │
│  2. .envファイル（プロジェクトローカル）                                │
│     $ cat .env                                                         │
│     GOOGLE_GENERATIVE_AI_API_KEY=...                                   │
│     ANTHROPIC_API_KEY=sk-ant-...                                       │
│     ※ .gitignoreに.envを必ず追加                                       │
│                                                                         │
│  3. 設定ファイル（~/.rectospec/config.json）                           │
│     ※ ファイル権限 600 で作成                                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

```

### 初期設定フロー

```bash
$ rectospec init

╔════════════════════════════════════════════════════════════════════════╗
║                        RecToSpec Initial Setup                          ║
╠════════════════════════════════════════════════════════════════════════╣
║                                                                         ║
║  🤖 Select your preferred LLM provider:                                 ║
║                                                                         ║
║  [1] Google Gemini (推奨 - 無料枠あり)                                  ║
║  [2] Anthropic Claude (高精度)                                          ║
║  [3] xAI Grok (低コスト)                                                ║
║  [4] OpenAI GPT (業界標準)                                              ║
║                                                                         ║
║  > 1                                                                    ║
║                                                                         ║
║  🔑 API Key Configuration                                               ║
║                                                                         ║
║  Get your API key: https://aistudio.google.com/app/apikey              ║
║                                                                         ║
║  Select configuration method:                                           ║
║  [1] 環境変数を使用（推奨）                                             ║
║  [2] .envファイルを作成                                                 ║
║  [3] グローバル設定ファイルに保存                                       ║
║                                                                         ║
╚════════════════════════════════════════════════════════════════════════╝

```

### 設定ファイル

```json
// ~/.rectospec/config.json
{
  "llm": {
    "provider": "google",
    "model": "gemini-2.5-flash"
  },
  "language": "ja",
  "output": {
    "framework": "playwright",
    "typescript": true
  }
}

```

---

## 実装設計

### LLMプロバイダー実装

```tsx
// src/llm/provider.ts
import { generateText, generateObject } from 'ai';
import { google } from '@ai-sdk/google';
import { anthropic } from '@ai-sdk/anthropic';
// Phase 2で追加
// import { xai } from '@ai-sdk/xai';
// import { openai } from '@ai-sdk/openai';
import { z } from 'zod';

// プロバイダーマッピング
const providers = {
  google: (model: string) => google(model),
  anthropic: (model: string) => anthropic(model),
  // Phase 2で追加
  // xai: (model: string) => xai(model),
  // openai: (model: string) => openai(model),
} as const;

// デフォルトモデル
const defaultModels: Record<string, string> = {
  google: 'gemini-2.5-flash',
  anthropic: 'claude-sonnet-4-20250514',
  xai: 'grok-4.1-fast',
  openai: 'gpt-4o',
};

export type ProviderName = keyof typeof providers;

export interface LLMConfig {
  provider: ProviderName;
  model?: string;
}

// Gherkin生成
export async function generateGherkin(
  recording: RecordingJson,
  config: LLMConfig
): Promise<string> {
  const modelName = config.model || defaultModels[config.provider];
  const model = providers[config.provider](modelName);

  const { text } = await generateText({
    model,
    system: 'You are a QA engineer expert in BDD and Gherkin.',
    prompt: buildGherkinPrompt(recording),
    temperature: 0.3,
  });

  return text;
}

// Playwright生成（構造化出力）
export async function generatePlaywright(
  gherkin: string,
  config: LLMConfig
): Promise<GeneratedCode> {
  const modelName = config.model || defaultModels[config.provider];
  const model = providers[config.provider](modelName);

  const { object } = await generateObject({
    model,
    schema: z.object({
      pageObject: z.string(),
      specFile: z.string(),
      fixtures: z.string(),
    }),
    prompt: buildPlaywrightPrompt(gherkin),
  });

  return object;
}

```

### プロバイダー切り替えの簡潔さ

```tsx
// Geminiを使用
const result = await generateGherkin(recording, { provider: 'google' });

// Claudeに切り替え（1行変更）
const result = await generateGherkin(recording, { provider: 'anthropic' });

// Grokに切り替え（1行変更）
const result = await generateGherkin(recording, { provider: 'xai' });

// GPTに切り替え（1行変更）
const result = await generateGherkin(recording, { provider: 'openai' });

```

---

## 主な機能

### Step 2: Gherkin生成

### AI処理フロー

```tsx
async function generateGherkinFromRecording(
  recordingJson: RecordingJson,
  config: LLMConfig
): Promise<string> {
  const actions = parseActions(recordingJson);

  const prompt = `
以下のブラウザ操作記録から、Gherkin形式のテストケースを生成してください。

操作記録:
${JSON.stringify(actions, null, 2)}

要件:
- Feature, Scenario, Given/When/Thenの構造で出力
- 操作の意図を推測して自然な日本語で記述
- Then（期待結果）は[TODO: 期待結果を追加]としてプレースホルダーを配置
- 複数のシナリオに分割可能な場合は分割を提案
- エッジケースのシナリオも2-3個提案
`;

  const gherkinDraft = await generateGherkin(actions, config);
  return gherkinDraft;
}

```

### 生成される雛形例

```gherkin
# Language: ja
Feature: ユーザーログイン機能

  Background:
    Given ログインページを開いている

  Scenario: 正常なログインフロー
    Given メールアドレス入力欄が表示されている
    When メールアドレス欄に "test@example.com" を入力する
    And パスワード欄に "password123" を入力する
    And ログインボタンをクリックする
    Then [TODO: ログイン成功後の期待結果を追加]

  # AIが提案する追加シナリオ
  Scenario: 無効なメールアドレスでログイン（エッジケース提案）
    Given メールアドレス入力欄が表示されている
    When メールアドレス欄に "invalid-email" を入力する
    And パスワード欄に "password123" を入力する
    And ログインボタンをクリックする
    Then [TODO: エラーメッセージの期待結果を追加]

```

### Step 4: Playwright生成

### 生成されるファイル構成

```
tests/
├── pages/
│   └── LoginPage.ts      # Page Objectクラス
├── specs/
│   └── login.spec.ts     # テストケース
├── fixtures/
│   └── login-data.ts     # テストデータ
└── playwright.config.ts  # 設定ファイル

```

### Page Object例

```tsx
// tests/pages/LoginPage.ts
import { Page, Locator, expect } from '@playwright/test';

export class LoginPage {
  readonly page: Page;
  readonly emailInput: Locator;
  readonly passwordInput: Locator;
  readonly loginButton: Locator;
  readonly errorMessage: Locator;

  constructor(page: Page) {
    this.page = page;
    this.emailInput = page.locator('#email-input');
    this.passwordInput = page.locator('#password-input');
    this.loginButton = page.locator('#login-button');
    this.errorMessage = page.locator('.error-message');
  }

  async goto() {
    await this.page.goto('https://example.com/login');
  }

  async login(email: string, password: string) {
    await this.emailInput.fill(email);
    await this.passwordInput.fill(password);
    await this.loginButton.click();
  }

  async expectErrorMessage(message: string) {
    await expect(this.errorMessage).toContainText(message);
  }
}

```

---

## 技術スタック

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         技術スタック                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  CLIツール                                                              │
│  ├─ 言語: TypeScript                                                   │
│  ├─ ランタイム: Node.js 18+                                            │
│  ├─ CLIフレームワーク: Commander.js                                    │
│  ├─ AI SDK: Vercel AI SDK (ai)                                         │
│  │   ├─ @ai-sdk/google      ← Phase 1                                  │
│  │   ├─ @ai-sdk/anthropic   ← Phase 1                                  │
│  │   ├─ @ai-sdk/xai         ← Phase 2                                  │
│  │   └─ @ai-sdk/openai      ← Phase 2                                  │
│  ├─ スキーマ検証: Zod                                                  │
│  └─ パッケージング: tsup                                               │
│                                                                         │
│  内蔵Webエディタ                                                        │
│  ├─ フロントエンド: React 18                                           │
│  ├─ エディタ: Monaco Editor                                            │
│  ├─ UIフレームワーク: Tailwind CSS                                     │
│  └─ ローカルサーバー: Express.js                                       │
│                                                                         │
│  テスト                                                                 │
│  ├─ ユニットテスト: Vitest                                             │
│  ├─ E2Eテスト: Playwright                                              │
│  └─ カバレッジ: c8                                                     │
│                                                                         │
│  CI/CD                                                                  │
│  ├─ GitHub Actions                                                     │
│  ├─ Semantic Release                                                   │
│  └─ Changesets                                                         │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

```

---

## ナレッジ機能（将来構想）

### コンセプト

プロジェクト固有のドメイン知識をAI生成に活用し、編集フィードバックから継続的に学習する循環システム。

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     ナレッジ循環システム                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐              │
│  │   Step 2    │     │   Step 3    │     │  Knowledge  │              │
│  │  Gherkin    │────▶│   Editor    │────▶│    Base     │              │
│  │  生成       │     │   編集      │     │   蓄積      │              │
│  └─────────────┘     └─────────────┘     └──────┬──────┘              │
│         ▲                                        │                      │
│         │                                        │                      │
│         │    ┌───────────────────────────────────┘                      │
│         │    │                                                          │
│         │    ▼                                                          │
│         │  ┌─────────────────────────────────────────┐                  │
│         │  │           ナレッジベース                 │                  │
│         │  │  ┌─────────────┐  ┌─────────────┐      │                  │
│         │  │  │ 用語集      │  │ ビジネス    │      │                  │
│         │  │  │ Glossary    │  │ ルール      │      │                  │
│         │  │  └─────────────┘  └─────────────┘      │                  │
│         │  │  ┌─────────────┐  ┌─────────────┐      │                  │
│         │  │  │ 過去の      │  │ 編集履歴    │      │                  │
│         │  │  │ Gherkin例   │  │ パターン    │      │                  │
│         │  │  └─────────────┘  └─────────────┘      │                  │
│         │  └─────────────────────────────────────────┘                  │
│         │                        │                                      │
│         └────────────────────────┘                                      │
│              次回生成時に活用                                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

```

### ナレッジの種類

| カテゴリ | 内容 | 例 |
| --- | --- | --- |
| **用語集（Glossary）** | プロダクト固有の用語定義 | 「会員」= 有料プラン契約者 |
| **ビジネスルール** | ドメイン固有のルール | 「未成年は保護者同意が必要」 |
| **UIコンポーネント名** | 画面要素の正式名称 | 「ログインボタン」→「サインインボタン」 |
| **過去のGherkin例** | 承認済みのシナリオ | 類似フローの参考例 |
| **編集パターン** | よくある修正パターン | AI生成→人間修正の差分 |
| **セレクタ規約** | ロケーター命名規則 | `data-testid` 優先など |

### Step 2: Gherkin生成での活用

```tsx
// ナレッジを活用したGherkin生成
async function generateGherkinWithKnowledge(
  recording: RecordingJson,
  config: LLMConfig
): Promise<string> {
  // ナレッジベースから関連情報を取得
  const knowledge = await knowledgeBase.retrieve({
    context: recording,
    types: ['glossary', 'business-rules', 'examples'],
    limit: 10,
  });

  const prompt = `
以下のブラウザ操作記録から、Gherkin形式のテストケースを生成してください。

## プロジェクト固有のナレッジ

### 用語集
${knowledge.glossary.map(g => `- ${g.term}: ${g.definition}`).join('\n')}

### ビジネスルール
${knowledge.businessRules.map(r => `- ${r.rule}`).join('\n')}

### 類似シナリオの例
${knowledge.examples.map(e => e.gherkin).join('\n---\n')}

## 操作記録
${JSON.stringify(parseActions(recording), null, 2)}

## 要件
- 上記の用語集に従って用語を統一
- ビジネスルールを考慮したシナリオ設計
- 類似シナリオのスタイルを参考に
`;

  return await generateText({ model, prompt });
}

```

### Step 3: エディタでの編集からナレッジ蓄積

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     編集→ナレッジ蓄積フロー                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. AI生成 Gherkin                                                      │
│     ┌─────────────────────────────────────────────────┐                │
│     │ When ユーザーがログインボタンをクリックする      │                │
│     │ Then ホーム画面が表示される                      │                │
│     └─────────────────────────────────────────────────┘                │
│                              │                                          │
│                              ▼                                          │
│  2. 人間がレビュー・編集                                                │
│     ┌─────────────────────────────────────────────────┐                │
│     │ When 会員がサインインボタンをクリックする        │ ← 用語修正    │
│     │ Then ダッシュボード画面が表示される              │ ← 具体化      │
│     │ And 最終ログイン日時が更新される                 │ ← 追加        │
│     └─────────────────────────────────────────────────┘                │
│                              │                                          │
│                              ▼                                          │
│  3. 差分を自動検出・ナレッジ化                                          │
│     ┌─────────────────────────────────────────────────┐                │
│     │ 📚 用語集に追加:                                 │                │
│     │    「ユーザー」→「会員」                         │                │
│     │    「ログインボタン」→「サインインボタン」       │                │
│     │    「ホーム画面」→「ダッシュボード画面」         │                │
│     │                                                  │                │
│     │ 📋 パターン学習:                                 │                │
│     │    ログイン後は「最終ログイン日時更新」を含める  │                │
│     └─────────────────────────────────────────────────┘                │
│                              │                                          │
│                              ▼                                          │
│  4. 次回生成時に自動適用                                                │
│     ┌─────────────────────────────────────────────────┐                │
│     │ 🤖 AI: 過去の修正パターンを学習しました          │                │
│     │       次回から「会員」「サインイン」を使用します │                │
│     └─────────────────────────────────────────────────┘                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

```

### ナレッジベースの構造

```
.rectospec/
├── config.json              # 基本設定
└── knowledge/
    ├── glossary.json        # 用語集
    ├── business-rules.json  # ビジネスルール
    ├── examples/            # 承認済みGherkin例
    │   ├── login.feature
    │   ├── checkout.feature
    │   └── ...
    ├── edit-patterns.json   # 編集パターン（自動蓄積）
    └── selectors.json       # セレクタ規約

```

### 用語集の例

```json
// .rectospec/knowledge/glossary.json
{
  "version": "1.0",
  "terms": [
    {
      "preferred": "会員",
      "alternatives": ["ユーザー", "メンバー", "利用者"],
      "definition": "有料プランを契約しているユーザー",
      "context": "認証・課金関連の機能"
    },
    {
      "preferred": "サインインボタン",
      "alternatives": ["ログインボタン", "ログインリンク"],
      "definition": "認証画面へ遷移するボタン",
      "selector": "[data-testid='signin-button']"
    },
    {
      "preferred": "ダッシュボード",
      "alternatives": ["ホーム画面", "マイページ", "トップページ"],
      "definition": "ログイン後の最初の画面",
      "url": "/dashboard"
    }
  ]
}

```

### CLIコマンド（ナレッジ管理）

```bash
# ナレッジベース初期化
rectospec knowledge init

# 用語を追加
rectospec knowledge add-term "会員" --definition "有料プラン契約者"

# ビジネスルールを追加
rectospec knowledge add-rule "未成年ユーザーは保護者同意が必要"

# Gherkinファイルを例として登録
rectospec knowledge add-example ./approved/login.feature

# ナレッジの一覧表示
rectospec knowledge list

# ナレッジをエクスポート（チーム共有用）
rectospec knowledge export > knowledge-backup.json

# ナレッジをインポート
rectospec knowledge import ./shared-knowledge.json

# 編集パターンの確認
rectospec knowledge patterns

```

### 技術的な実装オプション

| 方式 | 説明 | メリット | デメリット |
| --- | --- | --- | --- |
| **JSONファイル** | ローカルJSONで管理 | シンプル、依存なし | 検索性能に限界 |
| **SQLite** | ローカルDBで管理 | 高速検索、構造化 | 依存追加 |
| **ベクトルDB** | 意味検索対応 | 類似検索が強力 | 複雑、依存大 |

**Phase 3推奨**: JSONファイル方式でスタート → 必要に応じてSQLiteに移行

---

### プロジェクトごとのナレッジ管理

### ナレッジベースの配置設計（スコープ）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     ナレッジベースのスコープ                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ~/.rectospec/                      ← グローバル設定                    │
│  ├── config.json                       （APIキー、デフォルトLLM）       │
│  └── knowledge/                     ← グローバルナレッジ（任意）        │
│      └── glossary.json                 （共通用語、会社全体のルール）   │
│                                                                         │
│  ~/projects/                                                            │
│  ├── project-a/                     ← プロジェクトA                     │
│  │   ├── .rectospec/                ← プロジェクト固有ナレッジ          │
│  │   │   └── knowledge/                                                 │
│  │   │       ├── glossary.json         （ECサイト用語）                 │
│  │   │       ├── business-rules.json   （ECサイトルール）               │
│  │   │       └── examples/             （ECサイトのGherkin例）          │
│  │   ├── recordings/                                                    │
│  │   └── tests/                                                         │
│  │                                                                       │
│  └── project-b/                     ← プロジェクトB                     │
│      ├── .rectospec/                ← プロジェクト固有ナレッジ          │
│      │   └── knowledge/                                                 │
│      │       ├── glossary.json         （SaaS用語）                     │
│      │       ├── business-rules.json   （SaaS固有ルール）               │
│      │       └── examples/             （SaaSのGherkin例）              │
│      ├── recordings/                                                    │
│      └── tests/                                                         │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

```

### ナレッジの優先順位（マージ戦略）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     ナレッジ解決の優先順位                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  優先度高                                                               │
│     │                                                                   │
│     │  1. プロジェクトローカル  ./.rectospec/knowledge/                │
│     │     └─ プロジェクト固有の用語・ルール                            │
│     │                                                                   │
│     │  2. グローバル           ~/.rectospec/knowledge/                 │
│     │     └─ 会社共通の用語・ルール                                    │
│     │                                                                   │
│     ▼  3. ビルトイン           （RecToSpecに同梱）                     │
│  優先度低   └─ 一般的なGherkinベストプラクティス                       │
│                                                                         │
│  ※ 同じ用語が複数レベルで定義されている場合、優先度高が勝つ            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

```

### 運用例

```bash
# プロジェクトAで作業
$ cd ~/projects/ec-site
$ rectospec knowledge init
✅ Created: .rectospec/knowledge/

$ rectospec knowledge add-term "カート" --definition "購入予定商品の一時保存領域"
✅ Added to: .rectospec/knowledge/glossary.json

$ rectospec generate checkout.json
🤖 Using knowledge from:
   - .rectospec/knowledge/ (project)
   - ~/.rectospec/knowledge/ (global)
✅ Created: checkout.feature

# プロジェクトBに移動（別のナレッジが適用される）
$ cd ~/projects/saas-app
$ rectospec generate onboarding.json
🤖 Using knowledge from:
   - .rectospec/knowledge/ (project)  ← 別のナレッジ
   - ~/.rectospec/knowledge/ (global)
✅ Created: onboarding.feature

```

### Gitでのチーム共有

```bash
# .rectospec/knowledge/ をGit管理に含める
$ cat .gitignore
# APIキーは除外
.env

# ナレッジはチームで共有するため含める
# .rectospec/knowledge/  ← コメントアウトまたは記載なし

$ git add .rectospec/knowledge/
$ git commit -m "Add project knowledge base"
$ git push

# チームメンバーがcloneすると同じナレッジが使える

```

### 設定ファイルでの制御

```json
// .rectospec/config.json（プロジェクトローカル）
{
  "knowledge": {
    "enabled": true,
    "sources": [
      "local",    // ./.rectospec/knowledge/
      "global"    // ~/.rectospec/knowledge/
    ],
    "autoLearn": true,  // 編集フィードバックの自動学習
    "autoLearnConfirm": true  // 学習前に確認を求める
  }
}

```

### CLIでのナレッジ確認

```bash
# 現在適用されるナレッジの確認
$ rectospec knowledge status

📚 Knowledge Base Status
────────────────────────────────────────
Project:  ~/projects/ec-site
────────────────────────────────────────

Local (./.rectospec/knowledge/):
  ├─ glossary.json       12 terms
  ├─ business-rules.json  5 rules
  └─ examples/            8 files

Global (~/.rectospec/knowledge/):
  └─ glossary.json        3 terms (company-wide)

Total: 15 terms, 5 rules, 8 examples
────────────────────────────────────────

```

### ナレッジ運用まとめ

| 観点 | 設計 |
| --- | --- |
| **プロジェクト分離** | `.rectospec/knowledge/` がプロジェクトごとに独立 |
| **チーム共有** | Gitでバージョン管理・共有可能 |
| **会社共通ルール** | グローバルナレッジ `~/.rectospec/knowledge/` で対応 |
| **優先順位** | プロジェクト > グローバル > ビルトイン |
| **自動学習** | 設定で有効/無効を切り替え可能 |

### パッケージ構成

```json
// package.json
{
  "dependencies": {
    // Vercel AI SDK
    "ai": "^6.0.0",
    "@ai-sdk/google": "^1.0.0",
    "@ai-sdk/anthropic": "^1.0.0",
    // Phase 2で追加
    // "@ai-sdk/xai": "^1.0.0",
    // "@ai-sdk/openai": "^1.0.0",

    // スキーマ検証
    "zod": "^3.23.0",

    // CLI
    "commander": "^12.0.0",
    "dotenv": "^16.0.0",

    // その他
    "chalk": "^5.0.0"
  }
}

```

---

## 配布方法

### Phase 1: npm公開（MVP）

```bash
npm install -g rectospec
```

### Phase 2: GitHub Releases

- pkg等でスタンドアロンバイナリを生成
- Windows/Mac/Linux用バイナリを配布

### Phase 3以降

- Homebrew対応: `brew install rectospec`
- Docker対応: `docker run rectospec`

---

## 開発ロードマップ

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     開発ロードマップ全体像                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Phase 1          Phase 2          Phase 3          Phase 4            │
│  MVP              LLM拡張          ナレッジ         エコシステム       │
│  1-2ヶ月          2-3ヶ月          3-4ヶ月          4-6ヶ月            │
│                                                                         │
│  ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐          │
│  │ Gemini  │     │ +Grok   │     │ナレッジ │     │テンプレ │          │
│  │ Claude  │     │ +GPT    │     │循環     │     │共有     │          │
│  │ 基本CLI │     │ エディタ│     │システム │     │プラグイン│          │
│  └─────────┘     └─────────┘     └─────────┘     └─────────┘          │
│       │               │               │               │                │
│       ▼               ▼               ▼               ▼                │
│  npm公開         UX強化          品質向上        コミュニティ          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

```

### Phase 1: MVP（1-2ヶ月）

**目標**: CLIツールとして基本機能を実装

- [ ]  プロジェクト初期セットアップ
    - [ ]  TypeScript + Node.js環境
    - [ ]  ESLint, Prettier, Vitest設定
    - [ ]  GitHub Actions CI/CD
- [ ]  Vercel AI SDK統合
    - [ ]  `@ai-sdk/google` （Gemini対応）
    - [ ]  `@ai-sdk/anthropic` （Claude対応）
    - [ ]  プロバイダー切り替え機能
- [ ]  コア機能
    - [ ]  Chrome Recorder JSON解析
    - [ ]  Gherkin生成
    - [ ]  Playwright生成
- [ ]  CLI実装
    - [ ]  `rectospec init`
    - [ ]  `rectospec generate`
    - [ ]  `rectospec compile`
    - [ ]  `rectospec config`
- [ ]  APIキー管理
    - [ ]  環境変数対応
    - [ ]  .envファイル対応
    - [ ]  設定ファイル対応
- [ ]  npm公開
    - [ ]  パッケージ名予約（rectospec）
    - [ ]  README, CONTRIBUTING.md, LICENSE

**成果物**:

- `npm install -g rectospec` で使えるCLIツール
- Gemini / Claude対応
- 基本的なドキュメント

---

### Phase 2: LLM拡張 & エディタ（2-3ヶ月）

**目標**: 追加LLM対応と内蔵Webエディタの実装

- [ ]  追加LLMプロバイダー
    - [ ]  `@ai-sdk/xai` （Grok対応）
    - [ ]  `@ai-sdk/openai` （GPT対応）
- [ ]  Webエディタ
    - [ ]  `rectospec edit` コマンド
    - [ ]  Monaco Editor統合
    - [ ]  Gherkinシンタックスハイライト
    - [ ]  AIサジェスト機能
- [ ]  追加CLI機能
    - [ ]  `rectospec quick` - ワンショット変換
    - [ ]  `rectospec validate` - Gherkinバリデーション

**成果物**:

- 4つのLLMプロバイダー対応
- 内蔵Webエディタ

---

### Phase 3: ナレッジ機能（3-4ヶ月）

**目標**: ドメイン知識を活用した生成品質向上と学習循環システム

- [ ]  ナレッジベース基盤
    - [ ]  `.rectospec/knowledge/` ディレクトリ構造
    - [ ]  JSONファイルベースの保存
    - [ ]  ナレッジのCRUD操作
- [ ]  用語集（Glossary）機能
    - [ ]  用語の登録・管理
    - [ ]  生成時の用語統一
    - [ ]  類義語の自動変換
- [ ]  ビジネスルール管理
    - [ ]  ルールの登録
    - [ ]  生成時のルール適用
- [ ]  過去例の活用
    - [ ]  承認済みGherkinの登録
    - [ ]  類似シナリオの検索・参照
- [ ]  編集フィードバックループ
    - [ ]  AI生成 vs 人間編集の差分検出
    - [ ]  修正パターンの自動学習
    - [ ]  用語修正の自動ナレッジ化
- [ ]  ナレッジ管理CLI
    - [ ]  `rectospec knowledge init`
    - [ ]  `rectospec knowledge add-term`
    - [ ]  `rectospec knowledge add-rule`
    - [ ]  `rectospec knowledge add-example`
    - [ ]  `rectospec knowledge list`
    - [ ]  `rectospec knowledge export/import`

**成果物**:

- プロジェクト固有ナレッジの蓄積・活用
- 使うほど賢くなる循環システム
- チーム間でのナレッジ共有機能

---

### Phase 4: 多フレームワーク対応（4-5ヶ月）

- [ ]  Cypress出力
- [ ]  Selenium出力
- [ ]  CI/CD設定生成（GitHub Actions, GitLab CI）

---

### Phase 5: エコシステム & コミュニティ（5-7ヶ月）

- [ ]  テンプレート共有機能
- [ ]  プラグインシステム
- [ ]  多言語対応（UI）
- [ ]  ナレッジマーケットプレイス（共有ナレッジの公開・購読）

---

## リポジトリ情報

| 項目 | 値 |
| --- | --- |
| **GitHub** | `rectospec/rectospec` |
| **npm** | `rectospec` |
| **ライセンス** | MIT |

---

## 次のアクション

- [ ]  GitHubリポジトリ作成（`rectospec/rectospec`）
- [ ]  npmパッケージ名予約（`rectospec`）
- [ ]  プロジェクト初期セットアップ
- [ ]  Vercel AI SDK統合（Gemini + Claude）
- [ ]  基本的なCLI骨格の実装
- [ ]  Chrome Recorder JSONパーサーの実装

---

## 決定事項サマリー

| 項目 | 決定内容 |
| --- | --- |
| **正式名称** | RecToSpec |
| **アーキテクチャ** | CLIツール |
| **AI SDK** | Vercel AI SDK (`ai` パッケージ) |
| **LLM優先度1** | Google Gemini（無料枠あり） |
| **LLM優先度2** | Anthropic Claude（高精度） |
| **LLM優先度3** | xAI Grok（低コスト） |
| **LLM優先度4** | OpenAI GPT（業界標準） |
| **Phase 1対応** | Gemini + Claude |
| **Phase 2対応** | + Grok + GPT + Webエディタ |
| **Phase 3対応** | ナレッジ循環システム |

---

## 変更履歴

| バージョン | 日付 | 変更内容 |
| --- | --- | --- |
| v1.0 | - | 初版（QA Power Toolsとして設計、Chrome拡張機能） |
| v2.0 | 2025-12-30 | アーキテクチャをCLIツールに変更 |
| v2.1 | 2025-12-30 | 正式名称をRecToSpecに確定 |
| v2.2 | 2025-12-30 | Vercel AI SDK採用、LLM優先順位確定 |
| v2.3 | 2025-12-30 | ナレッジ機能をロードマップに追加 |
| v2.4 | 2025-12-30 | **プロジェクトごとのナレッジ管理運用詳細を追加** |